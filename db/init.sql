-- Optional manual database creation.
-- The JDBC URLs already include createDatabaseIfNotExist=true.
-- Run these statements manually if your MySQL user does not have CREATE DATABASE permission.

CREATE DATABASE IF NOT EXISTS employee_db;
CREATE DATABASE IF NOT EXISTS order_db;
